#!/usr/bin/env bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

if [ "$CPU_PROF" = "1" ]; then
  node --cpu-prof "$DIR/sharpurs.js" "$@"
else
  node --max-old-space-size=8192 "$DIR/sharpurs.js" "$@"
fi
