#!/usr/bin/env bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

if [ "$CPU_PROF" = "1" ]; then
  node --cpu-prof --expose-gc --stack-size=65536 "$DIR/sharpurs.js" "$@"
else
  node --expose-gc --stack-size=65536 --max-old-space-size=16384 "$DIR/sharpurs.js" "$@"
fi
