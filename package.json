{
  "name": "sharpurs",
  "version": "0.1.0",
  "description": "PureScript-to-F# backend",
  "type": "module",
  "bin": {
    "sharpurs": "bin/sharpurs.js"
  },
  "scripts": {
    "build": "spago build && NODE_OPTIONS=\"--no-deprecation\" purs-backend-es bundle-app -p node -m Main --to bin/sharpurs.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "esbuild": "^0.28.1",
    "purs-backend-es": "^1.4.3"
  },
  "devDependencies": {
    "purescript": "github:0x000000000000000000001/purescript-npm"
  }
}
